Why are ID selectors a bad idea?
    ID selectors have a high specificity which does not allow future developers to make any changes.

WHat selectors should you prefer when working with CSS?
    Some more preferable selectors are class,attribute,and pseudo selectors.